\section{系统设计}
\subsection{本章概述}
系统设计将需求分析结果转换为可实现的数据模型和系统结构。本章包括概念结构设计、E-R 图、逻辑结构设计、关系模式、系统功能模块图和其它设计图，保证后续 MySQL 表结构、后端 Entity、前端页面和报告描述一致。

\subsection{概念结构设计说明}
概念结构设计阶段从业务流程中抽取实体和联系。核心业务实体包括 visitor、visit\_apply、approval\_record、pass\_code、access\_record、blacklist、campus\_gate 等；权限实体包括 sys\_user、sys\_role、sys\_permission 以及用户角色、角色权限中间表；支撑实体包括 notice、operation\_log、dict\_type、dict\_item、screenshot\_record 和 report\_record。设计时将“访客提交预约”作为主线，确保每个实体都能在核心流程中找到业务来源。
\begin{longtable}{>{\raggedright\arraybackslash}p{0.300\textwidth}>{\raggedright\arraybackslash}p{0.640\textwidth}}
\caption{主要实体及存在原因}\\
\toprule
实体 & 存在原因 \\
\midrule
visitor & 保存访客身份基础信息，避免同一访客多次预约时重复录入。 \\
visit\_apply & 保存预约申请主记录，连接访客、被访人、部门、车辆、审批和出入校流程。 \\
approval\_record & 保存被访人确认、部门审批等多阶段审批过程。 \\
pass\_code & 保存审批通过后的通行凭证和有效期。 \\
access\_record & 保存门岗核验后的入校、离校和异常记录。 \\
blacklist & 保存限制或风险访客，支撑预约和核验阶段的安全检查。 \\
sys\_user/sys\_role/sys\_permission & 支撑系统登录认证、角色授权和菜单接口控制。 \\
operation\_log & 保存关键业务操作日志，支撑审计和测试说明。 \\
\bottomrule
\end{longtable}
\subsection{E-R 图}
为了避免一张图实体过多、线条交叉严重，报告将 E-R 图拆分为总体简化 E-R 图、核心业务 E-R 图、用户权限 E-R 图和系统支撑 E-R 图。正文重点展示总体关系和核心业务关系，权限与支撑实体作为补充说明。
\subsubsection{总体简化 E-R 图}

总体简化 E-R 图只展示主要实体和主流程关系，用于报告正文快速说明系统数据模型全貌。

\begin{figure}[H]

  \centering

  \includegraphics[width=0.94\textwidth]{figures/er_overview.png}

  \caption{总体简化 E-R 图}

\end{figure}\subsubsection{核心业务 E-R 图}

核心业务 E-R 图围绕访客预约、审批、通行凭证、出入校记录和黑名单展开，是后续关系模式转换的主要依据。

\begin{figure}[H]

  \centering

  \includegraphics[width=0.94\textwidth]{figures/er_core_business.png}

  \caption{核心业务 E-R 图}

\end{figure}\subsubsection{用户权限 E-R 图}

用户权限 E-R 图展示系统用户、角色、权限、部门及两个多对多中间表之间的联系。

\begin{figure}[H]

  \centering

  \includegraphics[width=0.94\textwidth]{figures/er_user_permission.png}

  \caption{用户权限 E-R 图}

\end{figure}\subsubsection{系统支撑 E-R 图}

系统支撑 E-R 图展示通知、日志、字典、截图记录和报告记录等支撑性实体。

\begin{figure}[H]

  \centering

  \includegraphics[width=0.94\textwidth]{figures/er_system_support.png}

  \caption{系统支撑 E-R 图}

\end{figure}
\subsection{逻辑结构设计说明}
逻辑结构设计将概念实体转换为 MySQL 关系表。实体表统一使用 \texttt{id} 作为主键，时间字段统一使用 \texttt{create\_time}、\texttt{update\_time}，软删除字段统一使用 \texttt{deleted}。多对多关系通过 sys\_user\_role 和 sys\_role\_permission 中间表表达；一对多关系通过外键字段或业务关联字段表达；预约、审批、通行和出入校状态通过状态字段表达。
\begin{longtable}{>{\raggedright\arraybackslash}p{0.220\textwidth}>{\raggedright\arraybackslash}p{0.520\textwidth}>{\raggedright\arraybackslash}p{0.200\textwidth}}
\caption{逻辑表结构摘要}\\
\toprule
表名 & 关键字段摘要 & 说明 \\
\midrule
sys\_user & id BIGINT；username VARCHAR(50)；password\_hash VARCHAR(255)；real\_name VARCHAR(50)；phone VARCHAR(20)；email VARCHAR(100)；department\_id BIGINT；user\_type VARCHAR(30) & 数据库物理表，与关系模式和后端 Entity 对应 \\
sys\_role & id BIGINT；role\_code VARCHAR(50)；role\_name VARCHAR(50)；role\_desc VARCHAR(255)；status VARCHAR(30)；create\_time DATETIME；update\_time DATETIME；deleted TINYINT & 数据库物理表，与关系模式和后端 Entity 对应 \\
sys\_permission & id BIGINT；permission\_code VARCHAR(100)；permission\_name VARCHAR(100)；permission\_type VARCHAR(30)；parent\_id BIGINT；route\_path VARCHAR(200)；component\_path VARCHAR(200)；api\_path VARCHAR(200) & 数据库物理表，与关系模式和后端 Entity 对应 \\
sys\_user\_role & id BIGINT；user\_id BIGINT；role\_id BIGINT；create\_time DATETIME；update\_time DATETIME；deleted TINYINT；PRIMARY KEY；UNIQUE KEY & 数据库物理表，与关系模式和后端 Entity 对应 \\
sys\_role\_permission & id BIGINT；role\_id BIGINT；permission\_id BIGINT；create\_time DATETIME；update\_time DATETIME；deleted TINYINT；PRIMARY KEY；UNIQUE KEY & 数据库物理表，与关系模式和后端 Entity 对应 \\
department & id BIGINT；parent\_id BIGINT；dept\_code VARCHAR(50)；dept\_name VARCHAR(100)；leader\_user\_id BIGINT；phone VARCHAR(20)；sort\_order INT；status VARCHAR(30) & 数据库物理表，与关系模式和后端 Entity 对应 \\
campus\_gate & id BIGINT；gate\_code VARCHAR(50)；gate\_name VARCHAR(100)；gate\_location VARCHAR(200)；gate\_type VARCHAR(30)；status VARCHAR(30)；remark VARCHAR(255)；create\_time DATETIME & 数据库物理表，与关系模式和后端 Entity 对应 \\
visitor & id BIGINT；visitor\_name VARCHAR(50)；id\_type VARCHAR(30)；id\_number VARCHAR(50)；phone VARCHAR(20)；company VARCHAR(100)；gender VARCHAR(10)；visitor\_level VARCHAR(30) & 数据库物理表，与关系模式和后端 Entity 对应 \\
visitor\_vehicle & id BIGINT；visitor\_id BIGINT；plate\_no VARCHAR(20)；vehicle\_type VARCHAR(30)；color VARCHAR(30)；brand VARCHAR(50)；status VARCHAR(30)；create\_time DATETIME & 数据库物理表，与关系模式和后端 Entity 对应 \\
visitor\_companion & id BIGINT；apply\_id BIGINT；companion\_name VARCHAR(50)；id\_type VARCHAR(30)；id\_number VARCHAR(50)；phone VARCHAR(20)；relation\_remark VARCHAR(100)；create\_time DATETIME & 数据库物理表，与关系模式和后端 Entity 对应 \\
visit\_apply & id BIGINT；apply\_no VARCHAR(50)；visitor\_id BIGINT；host\_user\_id BIGINT；department\_id BIGINT；vehicle\_id BIGINT；visit\_reason VARCHAR(255)；plan\_start\_time DATETIME & 数据库物理表，与关系模式和后端 Entity 对应 \\
approval\_record & id BIGINT；apply\_id BIGINT；approval\_step VARCHAR(30)；approver\_user\_id BIGINT；approval\_result VARCHAR(30)；approval\_comment VARCHAR(255)；approval\_time DATETIME；sort\_order INT & 数据库物理表，与关系模式和后端 Entity 对应 \\
pass\_code & id BIGINT；apply\_id BIGINT；pass\_code VARCHAR(64)；qr\_content VARCHAR(500)；valid\_from DATETIME；valid\_to DATETIME；pass\_status VARCHAR(30)；used\_time DATETIME & 数据库物理表，与关系模式和后端 Entity 对应 \\
access\_record & id BIGINT；apply\_id BIGINT；visitor\_id BIGINT；pass\_code\_id BIGINT；entry\_gate\_id BIGINT；exit\_gate\_id BIGINT；entry\_guard\_id BIGINT；exit\_guard\_id BIGINT & 数据库物理表，与关系模式和后端 Entity 对应 \\
blacklist & id BIGINT；visitor\_id BIGINT；id\_number VARCHAR(50)；phone VARCHAR(20)；reason VARCHAR(255)；level VARCHAR(30)；start\_time DATETIME；end\_time DATETIME & 数据库物理表，与关系模式和后端 Entity 对应 \\
notice & id BIGINT；receiver\_user\_id BIGINT；receiver\_type VARCHAR(30)；title VARCHAR(100)；content VARCHAR(1000)；business\_type VARCHAR(50)；business\_id BIGINT；read\_status VARCHAR(30) & 数据库物理表，与关系模式和后端 Entity 对应 \\
operation\_log & id BIGINT；operator\_user\_id BIGINT；operator\_name VARCHAR(50)；module\_name VARCHAR(100)；operation\_type VARCHAR(50)；request\_method VARCHAR(10)；request\_url VARCHAR(255)；operation\_result VARCHAR(30) & 数据库物理表，与关系模式和后端 Entity 对应 \\
dict\_type & id BIGINT；type\_code VARCHAR(50)；type\_name VARCHAR(100)；status VARCHAR(30)；remark VARCHAR(255)；create\_time DATETIME；update\_time DATETIME；deleted TINYINT & 数据库物理表，与关系模式和后端 Entity 对应 \\
dict\_item & id BIGINT；type\_id BIGINT；item\_code VARCHAR(50)；item\_name VARCHAR(100)；item\_value VARCHAR(100)；sort\_order INT；status VARCHAR(30)；remark VARCHAR(255) & 数据库物理表，与关系模式和后端 Entity 对应 \\
screenshot\_record & id BIGINT；screenshot\_code VARCHAR(50)；page\_name VARCHAR(100)；route\_path VARCHAR(200)；role\_code VARCHAR(50)；file\_path VARCHAR(255)；description VARCHAR(255)；capture\_time DATETIME & 数据库物理表，与关系模式和后端 Entity 对应 \\
report\_record & id BIGINT；report\_code VARCHAR(50)；report\_name VARCHAR(100)；markdown\_path VARCHAR(255)；word\_path VARCHAR(255)；generate\_status VARCHAR(30)；generate\_time DATETIME；generated\_by BIGINT & 数据库物理表，与关系模式和后端 Entity 对应 \\
\bottomrule
\end{longtable}
\subsection{关系模式说明}
系统关系模式与 MySQL 表结构一一对应。核心关系模式包括 visitor(id, visitor\_name, phone, id\_number, ...)、visit\_apply(id, apply\_no, visitor\_id, host\_user\_id, department\_id, apply\_status, access\_status, ...)、approval\_record(id, apply\_id, approval\_step, approval\_result, ...)、pass\_code(id, apply\_id, code, valid\_from, valid\_to, status, ...)、access\_record(id, apply\_id, visitor\_id, entry\_gate\_id, exit\_gate\_id, access\_status, ...)。这些关系模式既能表达实体属性，也能表达核心业务联系。

\subsection{系统功能模块图}
系统功能模块图按角色和业务域划分为访客端、被访人端、审批管理、门岗管理、访客记录、安全管理、统计报表、系统管理和自动化支撑等模块。
\subsubsection{系统功能模块图}

模块图展示系统功能分解及各模块边界，便于说明前端菜单、后端接口和权限控制的对应关系。

\begin{figure}[H]

  \centering

  \includegraphics[width=0.94\textwidth]{figures/system_module.png}

  \caption{系统功能模块图}

\end{figure}\subsection{其它设计图}\subsubsection{访客预约审批流程图}

访客预约审批流程图展示从预约提交到审批通过或拒绝的状态流转，并体现黑名单拦截、被访人拒绝和部门审批拒绝等异常分支。

\begin{figure}[H]

  \centering

  \includegraphics[width=0.94\textwidth]{figures/visitor_workflow.png}

  \caption{访客预约审批流程图}

\end{figure}\subsubsection{门岗核验入校流程图}

门岗核验流程图展示通行码核验、时间有效性检查、黑名单检查、入校登记、离校登记和超时未离校处理。

\begin{figure}[H]

  \centering

  \includegraphics[width=0.94\textwidth]{figures/gate_check_workflow.png}

  \caption{门岗核验入校流程图}

\end{figure}\subsubsection{系统架构图}

系统架构图展示浏览器前端、Spring Boot 后端、MySQL 数据库、Playwright 截图脚本和 LaTeX 报告生成脚本之间的关系。

\begin{figure}[H]

  \centering

  \includegraphics[width=0.94\textwidth]{figures/system_architecture.png}

  \caption{系统架构图}

\end{figure}\subsubsection{数据库表关系图}

数据库表关系图适合放入附录，用于展示主外键关系和表间依赖。

\begin{figure}[H]

  \centering

  \includegraphics[width=0.94\textwidth]{figures/table_relation.png}

  \caption{数据库表关系图}

\end{figure}\subsubsection{自动截图与报告生成流程图}

自动化支撑流程图展示演示数据、页面截图和报告生成之间的衔接关系，说明系统如何形成可复核的课程设计材料。

\begin{figure}[H]

  \centering

  \includegraphics[width=0.94\textwidth]{figures/automation_report_workflow.png}

  \caption{自动截图与报告生成流程图}

\end{figure}

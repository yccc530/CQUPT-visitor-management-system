interface:
  display_name: "认证与 RBAC 权限"
  short_description: "设计并实现登录认证、角色权限和前后端访问控制"
  default_prompt: "Use $auth-rbac-builder to implement authentication and role-based access control."

policy:
  allow_implicit_invocation: true
